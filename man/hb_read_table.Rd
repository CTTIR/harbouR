% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rows.R
\name{hb_read_table}
\alias{hb_read_table}
\title{Read a table as a tibble}
\usage{
hb_read_table(
  client,
  table,
  view = NULL,
  limit = 1000L,
  call = rlang::caller_env()
)
}
\arguments{
\item{client}{A \code{harbour_client}.}

\item{table}{Name of the table.}

\item{view}{Optional view name.}

\item{limit}{Page size for paginated fetches. Default \code{1000}.}

\item{call}{Internal: error-propagation env.}
}
\value{
A tibble with one row per SeaTable row and one column per
SeaTable column, plus \verb{_id} (chr). A 0-row tibble is returned for an
empty table.
}
\description{
Reads all rows from \code{table} (optionally filtered by \code{view}) and returns
them as a typed tibble. Pagination is handled internally; the returned
tibble always has the table's columns in declared order plus an \verb{_id}
column.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
client <- hb_client()
hb_read_table(client, "Samples")
\dontshow{\}) # examplesIf}
}
\seealso{
Other rows:
\code{\link[=hb_append_rows]{hb_append_rows()}},
\code{\link[=hb_delete_rows]{hb_delete_rows()}},
\code{\link[=hb_get_row]{hb_get_row()}},
\code{\link[=hb_lock_rows]{hb_lock_rows()}},
\code{\link[=hb_query]{hb_query()}},
\code{\link[=hb_unlock_rows]{hb_unlock_rows()}},
\code{\link[=hb_update_rows]{hb_update_rows()}}
}
\concept{rows}
