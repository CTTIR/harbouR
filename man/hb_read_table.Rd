% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dtable-methods.R, R/rows.R
\name{hb_read_table.harbour_dtable}
\alias{hb_read_table.harbour_dtable}
\alias{hb_read_table.default}
\alias{hb_read_table}
\alias{hb_read_table.harbour_client}
\title{Read a table as a tibble}
\usage{
\method{hb_read_table}{harbour_dtable}(x, table, ..., view = NULL, n_max = Inf, option_labels = TRUE)

\method{hb_read_table}{default}(x, table, ...)

hb_read_table(x, table, ...)

\method{hb_read_table}{harbour_client}(x, table, ..., view = NULL, page_size = 1000L, n_max = Inf)
}
\arguments{
\item{x}{A \code{harbour_client} connected to a base, or a
\code{harbour_dtable} read from a local file.}

\item{table}{Name of the table.}

\item{...}{These dots are for future extensions and must be empty.}

\item{view}{Optional view name.}

\item{n_max}{Maximum number of rows to return. \code{Inf}, the default,
reads the whole table.}

\item{option_labels}{Translate select-option ids to their display
names. On disk a select cell holds an option id; over the API it
holds the name, so this is what makes a local read and a server read
of the same column agree. Set \code{FALSE} to see the raw ids.}

\item{page_size}{Rows fetched per request. SeaTable caps this at 1000
and harbouR clamps it, warning if you asked for more.}
}
\value{
A tibble with one row per SeaTable row and one column per
SeaTable column, plus \verb{_id} (chr). A 0-row tibble is returned for an
empty table.
}
\description{
Reads all rows from \code{table} (optionally filtered by \code{view}) and returns
them as a typed tibble. Pagination is handled internally; the returned
tibble always has the table's columns in declared order plus an \verb{_id}
column.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
client <- hb_client()
hb_read_table(client, "Samples")

# just the first 10 rows
hb_read_table(client, "Samples", n_max = 10)
\dontshow{\}) # examplesIf}
}
\seealso{
Other rows:
\code{\link[=hb_append_rows]{hb_append_rows()}},
\code{\link[=hb_delete_rows]{hb_delete_rows()}},
\code{\link[=hb_get_row]{hb_get_row()}},
\code{\link[=hb_lock_rows]{hb_lock_rows()}},
\code{\link[=hb_query]{hb_query()}},
\code{\link[=hb_unlock_rows]{hb_unlock_rows()}},
\code{\link[=hb_update_rows]{hb_update_rows()}}
}
\concept{rows}
