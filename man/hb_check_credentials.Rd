% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{hb_check_credentials}
\alias{hb_check_credentials}
\title{Check that the client's credentials are accepted}
\usage{
hb_check_credentials(client, ...)
}
\arguments{
\item{client}{A \code{harbour_client}.}

\item{...}{These dots are for future extensions and must be empty.}
}
\value{
The \code{client}, invisibly. Errors with a \code{harbour_error_auth}
condition if the credentials are rejected.
}
\description{
Exchanges the client's credentials for a token, which is the cheapest
way to find out whether they work. For an API-token client this fetches
a base token, and so also confirms the token is valid \emph{for that base};
for a username/password client it fetches an account token.
}
\details{
Unlike \code{\link[=hb_ping]{hb_ping()}}, which only proves the server is up, this proves the
credentials are usable.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
client <- hb_client()
hb_check_credentials(client)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=hb_ping]{hb_ping()}}

Other client:
\code{\link[=hb_client]{hb_client()}},
\code{\link[=hb_ping]{hb_ping()}},
\code{\link[=hb_server_info]{hb_server_info()}},
\code{\link[=is_harbour_client]{is_harbour_client()}}
}
\concept{client}
