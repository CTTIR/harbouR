% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rows.R
\name{hb_query}
\alias{hb_query}
\title{Run a SeaTable SQL query}
\usage{
hb_query(client, sql, ..., parameters = NULL, convert_keys = TRUE)
}
\arguments{
\item{client}{A \code{harbour_client}.}

\item{sql}{SeaTable SQL query string. SeaTable applies an implicit
\verb{LIMIT 100} when the query has none, and caps results at 10000 rows.
harbouR warns if there is no \code{LIMIT} clause.}

\item{...}{These dots are for future extensions and must be empty.}

\item{parameters}{Optional list of values for \verb{?} placeholders in \code{sql}.}

\item{convert_keys}{Return column names rather than column keys.
Default \code{TRUE}.}
}
\value{
A tibble, typed from the result schema SeaTable reports
alongside the rows, so the same table read via \code{\link[=hb_read_table]{hb_read_table()}} and
via \code{hb_query()} yields the same column types. Always a tibble, even
when the query returns no rows.
}
\description{
Run a SeaTable SQL query
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
client <- hb_client()
hb_query(client, "select * from Samples limit 5")
\dontshow{\}) # examplesIf}
}
\seealso{
Other rows:
\code{\link[=hb_append_rows]{hb_append_rows()}},
\code{\link[=hb_delete_rows]{hb_delete_rows()}},
\code{\link[=hb_get_row]{hb_get_row()}},
\code{\link[=hb_lock_rows]{hb_lock_rows()}},
\code{\link[=hb_read_table]{hb_read_table()}},
\code{\link[=hb_unlock_rows]{hb_unlock_rows()}},
\code{\link[=hb_update_rows]{hb_update_rows()}}
}
\concept{rows}
