% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/files.R
\name{hb_upload_file}
\alias{hb_upload_file}
\title{Upload a file to SeaTable}
\usage{
hb_upload_file(
  client,
  path,
  relative_path = "files",
  call = rlang::caller_env()
)
}
\arguments{
\item{client}{A \code{harbour_client}.}

\item{path}{Local file path. Must exist.}

\item{relative_path}{Optional path on the SeaTable side; defaults to
\code{"files"}.}

\item{call}{Internal: error-propagation env.}
}
\value{
A named list describing the uploaded asset.
}
\description{
Uploads \code{path} to the active base and returns the SeaTable file object
(a named list with \code{name}, \code{size}, \code{type} and \code{url}) ready to be written
into a \code{file}-typed cell via \code{\link[=hb_update_rows]{hb_update_rows()}} or \code{\link[=hb_attach_file]{hb_attach_file()}}.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
client <- hb_client()
hb_upload_file(client, "report.pdf")
\dontshow{\}) # examplesIf}
}
\seealso{
Other files:
\code{\link[=hb_attach_file]{hb_attach_file()}},
\code{\link[=hb_delete_asset]{hb_delete_asset()}},
\code{\link[=hb_download_file]{hb_download_file()}}
}
\concept{files}
