% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{hb_client}
\alias{hb_client}
\alias{print.harbour_client}
\title{Create a SeaTable client}
\usage{
hb_client(
  server = Sys.getenv("SEATABLE_SERVER"),
  api_token = Sys.getenv("SEATABLE_API_TOKEN"),
  username = NULL,
  password = NULL,
  base_uuid = NULL,
  timeout = 30,
  call = rlang::caller_env()
)

\method{print}{harbour_client}(x, ...)
}
\arguments{
\item{server}{SeaTable server URL, e.g. \code{"https://cloud.seatable.io"}.
Defaults to the \code{SEATABLE_SERVER} env var.}

\item{api_token}{Long-lived per-base API token. Defaults to the
\code{SEATABLE_API_TOKEN} env var. Mutually exclusive with
\code{username}/\code{password}.}

\item{username, password}{Account credentials. Used to acquire an account
token. Mutually exclusive with \code{api_token}.}

\item{base_uuid}{Optional base UUID hint; usually discovered from the
base-token exchange.}

\item{timeout}{Per-request timeout in seconds. Default \code{30}.}

\item{call}{Internal: error-propagation env. Not for direct use.}

\item{x}{A \code{harbour_client}.}

\item{...}{Unused.}
}
\value{
A \code{harbour_client} object.
}
\description{
Builds a \code{harbour_client} object that holds the server URL, the user's
credentials and a lazily-fetched, transparently-refreshed base token.
The client is environment-backed, so internal token refreshes mutate
cached state in place without reassigning your variable.
}
\details{
SeaTable uses a three-token model. \code{hb_client()} accepts either an
\strong{API token} (per-base, long-lived, generated in the SeaTable UI) or
\strong{username + password} (which yields an account token). In-base calls
transparently exchange these for a short-lived \strong{base token}.

Defaults for \code{server} and \code{api_token} are read from the environment
variables \code{SEATABLE_SERVER} and \code{SEATABLE_API_TOKEN}.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
client <- hb_client(
  server = "https://cloud.seatable.io",
  api_token = Sys.getenv("SEATABLE_API_TOKEN")
)
print(client)
\dontshow{\}) # examplesIf}
}
\seealso{
Other client:
\code{\link[=hb_ping]{hb_ping()}},
\code{\link[=hb_server_info]{hb_server_info()}},
\code{\link[=is_harbour_client]{is_harbour_client()}}
}
\concept{client}
